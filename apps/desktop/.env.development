# ⚠️ WARNING: This file is for LOCAL DEVELOPMENT only.
# NEVER commit real secrets or API keys here.
# Production config is managed via environment variables or vault.

VITE_API_BASE_URL=/api
VITE_WORKER_BASE_URL=/worker
VITE_APP_MODE=dev
