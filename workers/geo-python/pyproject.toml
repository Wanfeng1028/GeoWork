[project]
name = "geowork-geo-python-worker"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "pydantic", "python-docx", "earthengine-api", "geemap", "pandas", "numpy", "pytest", "httpx"]
